[project]
name = "chessminds-dashboard"
version = "0.0.1"
description = "Local analytics dashboard for Chess of Minds."
requires-python = ">=3.12"
dependencies = [
  "boto3>=1.34",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
]

[project.scripts]
chessminds-dashboard = "main:run"
